import 'package:flutter/material.dart';

class ByObraTab extends StatelessWidget {
  const ByObraTab({super.key});

  @override
  Widget build(BuildContext context) => const Center(child: Text("Visão por Obra - Em desenvolvimento"));
}